export default class HashNode {
    public key: number;
    public complement: number;

    constructor(key: number, complement: number) {
        this.key = key;
        this.complement = complement;
    }
}