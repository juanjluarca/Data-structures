export default class Student {
    public name: string;
    public id: string;
    public average: number;

    constructor(name: string, id: string, average: number) {
        this.name = name;
        this.id = id;
        this.average = average;
    }
}
